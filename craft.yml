php-version: 8.4
extensions: "bcmath,filter,mbstring,mbregex,phar,zlib,openssl,tokenizer,xml"
sapi: micro
download-options:
  prefer-pre-built: true
build-options:
  enable-zts: true
  with-hardcoded-ini: ["memory_limit=2G", "phar.readonly=0"]