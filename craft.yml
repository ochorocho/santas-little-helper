php-version: 8.4
# @todo: Add all extensions required for TYPO3 e.g. redis?!
extensions: "bcmath,filter,mbstring,mbregex,phar,zlib,openssl,tokenizer,posix,simplexml,xml,dom"
sapi: micro,frankenphp
download-options:
  prefer-pre-built: true
build-options:
  enable-zts: true
  with-hardcoded-ini: ["memory_limit=4G"]