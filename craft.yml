php-version: 8.4
extensions: "bcmath,filter,mbstring,mbregex,phar,zlib,openssl,tokenizer,posix,simplexml,xml,dom,xml,zip,zlib,pdo,intl,curl,iconv,session,gd,fileinfo,exif,mysqlnd,pdo,pdo_mysql"
libs: freetype,libavif,libjpeg,libwebp,brotli
sapi: micro,frankenphp
download-options:
  prefer-pre-built: true
build-options:
  enable-zts: true
  with-hardcoded-ini: ["memory_limit=4G", "max_input_vars=1500"]
